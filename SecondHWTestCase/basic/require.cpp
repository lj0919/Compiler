int main()
{
	int a = 1, b = a, _ = _, _1, _2, _3; 
	int (d) = (1), e = (1), (f) = d; // May Be tolerated at first time if ...
	char c = c;
	if( c = 1 )
	{
		c = _ + c * c - c / c;
		a = (b >= a) <= (a == a);
		
		c = (c, c) = c; // Can be tolerated if failed, please notate it
		(a, b, c) = (a, b, c) = (_, _, _); // However, equal c = c = c;
	}
	else if ( c != 1)
		if ( c > 1) 
			_1 >= _2;

	
	{
		{
			c = 1 + 2 - 3 / 4 * 5 - 3 <= 4; // c = 1
		}
	}
	
	read(c); // Anyway
	write(c);
}
